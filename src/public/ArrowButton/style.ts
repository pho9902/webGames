import styled from "styled-components";

export const Arrow = styled.button`
  background: white;
  padding: 10px;
  width: 60px;
  height: 60px;
  margin: 10px 40px;
  &:hover {
    filter: brightness(97%);
  }
`;
